import { counterReducer } from './model/slice/counter_slice';
import { Counter } from './ui/counter';
import type { CounterScheme } from './model/types/counter_scheme';

export {
    counterReducer,
    Counter,
    CounterScheme,
};
