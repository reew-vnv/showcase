import { counterReducer } from './model/slice/counter-slice';
import { Counter } from './ui/counter';
import type { CounterScheme } from './model/types/counter-scheme';

export {
    counterReducer,
    Counter,
    CounterScheme,
};
